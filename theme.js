import { DefaultTheme } from 'react-native-paper';

export default theme = {
    ...DefaultTheme,
    roundness: 30,
    colors: {
        ...DefaultTheme.colors,
        primary: '#dc143c',
        accent: '#f1c40f',
        headerBarBg: '#f7f7f7'
    },
};